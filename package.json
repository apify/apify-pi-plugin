{
  "name": "apify-pi-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Apify integration plugin for the pi agent",
  "scripts": {
    "setup:pi": "bash scripts/setup.sh",
    "cleanup:pi": "rm -rf .pi-install ~/.pi/agent/auth.json ~/.pi/agent/apify.json 2>/dev/null; true"
  }
}
